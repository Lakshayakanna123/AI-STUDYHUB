# Leave OPENAI_API_KEY blank to run the AI service in mock mode (safe default for testing).
# Add a real key to enable actual Whisper transcription + GPT question generation/evaluation/chat.
OPENAI_API_KEY=
MONGO_URI=mongodb://localhost:27017/virtual-classroom
